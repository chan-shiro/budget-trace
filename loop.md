# loop.md — `/loop` で収録を自律的に進めるときの「1巡」の手順書

起動は `/loop loop.md に従って次の1巡を進める`（間隔は書かない＝自己ペース）。
このファイルは **人が見ていない間に、収録の仕事を1巡ずつ安全に進める**ための手順書で、
**知識は持たない**。知識の正は CLAUDE.md → [docs/handoff.md](docs/handoff.md) →
[docs/data-sources.md](docs/data-sources.md)。ここに書くのは **順番・境界・止まり方** だけ。
ここに書いてあることと docs が食い違ったら docs が正しく、このファイルを直す。

## 設定（人が変えるのはここだけ）

| 項目 | 既定 | 備考 |
| --- | --- | --- |
| 1巡の単位 | **1つの偵察バッチ（最大＝偵察の並列数）＝ 1本の PR** | 都道府県キャンペーンのリズム。偵察レポートはセッション限りなので、同じ巡で収録し切る |
| 仕事の優先順 | **横展開（未収録の団体を増やす）＞ 既収録団体の主な事業・事業報告を揃える ＞ 年度延伸はしない** | 2026-07-25 の指示。年度は「できる/できない」を記録するだけ |
| 偵察の並列数 | 最大 **5** 体（自治体ごとに1体） | 都道府県を一巡したときのリズム（handoff §5-0） |
| マージ | **CI 緑＋レビュー所見ゼロなら自分で squash する** | `/loop` 中だけの取り決め。条件は §3。`--delete-branch` は付けない |
| レビュー | **収録の巡では省略しない**。所見が出なくなるまで回す | `ingest-reviewer`（model 固定）を起動する |

## 1巡の境界

- **1巡 ＝ 同時に偵察した団体をまとめて収録する1本の PR**（第1巡の実測: 5市を偵察して5市とも収録可だった。
  偵察結果を次巡に持ち越すと再偵察になるので、その巡で収録し切る）。**資料種別は混ぜない** —
  款別（＋同型の過年度）だけにし、主な事業・事業報告は偵察結果を docs に残して別の巡にする。
  新しい団体では、偵察が同型と実測した過年度は同じ巡で入れてよい（年度延伸の禁止は「年度を増やすために
  巡を使わない」の意味）。パーサ改修が要る年度は入れず、理由を registry のコメントと docs に残す。
- **1巡で新しいキャンペーン（中核市・県の事業報告 など）を2つ以上始めない**。
- 巡の長さは仕事で決まる（収録は数時間かかってよい）。**待つのは CI と Wayback だけ**。

## 0. 巡の開始（毎回・省略しない）

1. `git fetch origin main`。作業ブランチが遅れていたら取り込む。**main 側でパーサが変わっていたら
   自分の新規ソースを再 parse して差分ゼロを確認する**（ワークツリーの base は古いことがある）。
2. **初回の巡だけ** handoff を通読する。2巡目以降は §2 冒頭の表と §5 だけ読み直す
   （件数の正は `public/coverage.json`。docs の数字は追記時点のもの）。
3. **後始末を先に**: `gh pr list --author @me --state open`
   - CI 赤 → 直す（この巡はそれで終わってよい）
   - レビュー所見が残っている → 自分で再現してから直し、もう1巡レビューする
   - CI 緑＋所見ゼロ → §3 の条件でマージする
   - **未マージの PR の上に次の PR を積まない**。次の仕事は `origin/main` から新しいブランチで始める。
4. `gh issue list --state open` で `収録` / `偵察` / `パーサ` / `パイプライン` ラベルを見る。

## 1. 仕事の選び方（上から順に見て、最初に当たった **1つ** を選ぶ）

- **a. 自分の未マージ PR の後始末**（§0-3）。
- **b. open issue**。ピン留め → ラベル `収録` / `偵察` / `パーサ` / `パイプライン` の順。
  ⚠ **年度延伸の issue（#166 型）は着手しない**。⚠ **`設計` ラベルは人の判断が要る**ので触らない。
- **c. issue が無ければ、設定の優先順で候補を作る。候補は手書きしない — 毎回計算する**:
  - **c-1. 未収録の市区町村を人口順に**（総務省の人口 × `BUDGET_SOURCES` に無い団体。
    `unrecordable.ts` にある団体は理由を読んでから判断する — 判定は実際にくつがえる）:

    ```bash
    python3 - <<'PY'
    import json, re
    recs = json.load(open('data/normalized/municipal-accounts/R6.json'))['records']
    have = set(re.findall(r'muniCode:\s*"(\d{6})"', open('pipeline/derive-app-data.ts').read()))
    unrec = set(re.findall(r'code:\s*"(\d{6})"', open('pipeline/registry/unrecordable.ts').read()))
    rows = sorted((r for r in recs if r['muniCode'] not in have and r.get('population')),
                  key=lambda r: -r['population'])
    for r in rows[:10]:
        print(r['muniCode'], r['muniName'], r['population'], '← unrecordable.ts に記録あり' if r['muniCode'] in unrec else '')
    PY
    ```

  - **c-2. 既収録団体で欠けている資料種別**（主な事業・事業報告）。どこが欠けているかは gen で数える
    （`MUNI_BUDGET_YEARS[code][].projects.length` と `REPORT_MUNIS`）。申し送りは handoff §5-0a/0b と
    data-sources.md §11l 末尾（県5つぶん）。⚠ **「次は○○」と団体名が書いてあっても信じず、gen で
    数えてから着手する**（浜松・北九州は「次は」と書かれたまま収録済みだった実例）。
- **d. 候補が尽きたら止まる**（§5）。

## 2. 1巡の手順

### 偵察が要るとき

- `source-scout` サブエージェントを **自治体ごとに1体・設定の並列数まで**起動する。プロンプトの雛形は
  handoff §11k・§11l（罠の一覧と `noDeepLink` の5群つき）。**団体コードは書かずに自分で引かせる**。
  既定の **4点セット**（①款別＋前年当初比較 ②過年度 ③主な事業 ④事業報告）で起動し、「款別だけ」に絞らない。
- **偵察レポートはセッション限りで消える**。この巡で収録しない自治体のぶんは、直リンク URL・物理ページ・
  見出し語・ライセンス原文まで data-sources.md の該当節へそのまま貼ってから終える。
- 偵察の「款名クリーン」「現存年度数」「団体コード」は**信用しない**。収録時に自分で実測する
  （年度の網羅性は発行元の年度一覧を自分で辿る。ただし年度延伸は着手しない）。

### 収録

- `/ingest-source` を最後まで通す（registry 登録 → fetch → parse → validate → archive → derive →
  アプリ配線 → docs 追記）。手順を省いて近道しない。
- **`pipeline:archive` は直列**（複数プロセスを同時に走らせない。台帳を丸ごと上書きする）。
  Wayback のレート制限に当たったら残りは次巡へ回し、台帳を自分で開いて登録数を確認する（件数の報告を信じない）。
- **団体コードは総務省の原本から引く**（偵察の申告もプロンプトの数字も根拠にしない）。
- **款名・事業名は全件目視する**（validate は Σ しか見ない。表示専用フィールドのゲートは目視の代わりにならない）。
- パーサ本体を触ったら**同じパーサの既存全ソースを再 parse し、`parsedAt` 以外の差分ゼロ**を確認する。
- `open` は誤ると一番危ない向き。license 欄には**適用される条件だけ**を原文のまま書く。

### レビュー（収録の巡では必ず）

- `ingest-reviewer` を起動 → 所見を**自分で再現してから**直す → 「何を直したか・何を見てほしいか」を書いて
  再起動 → **所見が「軽微」「PLAUSIBLE」だけになるまで**繰り返す。前の巡の修正が次の穴を作る。

### PR

- ブランチは `origin/main` から `claude/<slug>`。
- squash されるので**タイトルと本文だけで読める**ようにする: 何を・何件・何を踏んだか・docs のどこに書いたか。
  issue があれば `Closes #N`。
- CI（`.github/workflows/ci.yml`）の緑を待つ: `gh pr checks <n> --watch`（実測で数分）。

## 3. マージ（設定が「自分で squash する」のとき）

次の**すべて**が揃ったときだけ:

1. CI 緑
2. レビューの所見ゼロ（収録の巡）
3. `git fetch origin main` して base との差を見る。**main 側が registry / data / gen を触っていたら
   `bun run pipeline:derive` を回し直して `bun run typecheck` まで通す**（生成物は競合せず合流するので
   PR の緑はマージ結果のビルドではない）
4. 積んだ PR が無い

```bash
gh pr merge <n> --squash
```

マージ後は**本番デプロイの成否を見届けるまでが作業**:
`gh api repos/chan-shiro/budget-trace/commits/$(git rev-parse origin/main)/status`。
失敗したら次の巡はその修復（新しい仕事を始めない）。

## 4. 巡の終わり方

- **docs を更新してから終える**: data-sources.md（資料のクセ・発見）、handoff §2/§5（実態の変化）。
  **件数・進捗の数字を手書きしない**（書くなら測った日付つき）。`roadmap.ts` に時期・数字・done を書かない。
- ユーザーへの報告（短く）: やったこと／PR 番号／次巡の着手点／人の判断が要ること。
- 次の巡の間隔: 次の仕事があるなら短く（60s）。CI 待ちは実測に合わせる。Wayback のレート制限待ちは 1200s 以上。

## 5. 止まる（`stop`）条件

- 候補が尽きた（open issue が 0 件で §1-c の候補も空）。
- **同じ作業で2巡連続して失敗した**。
- **人の判断が要る**: ライセンス区分が割れる／raw 肥大の崖（事業報告の全量・大きな資料）／
  新 docType・新パーサの設計／`設計` ラベル／main の本番デプロイが壊れていて原因が自分の変更でない。
  ⚠ **バッチの一部の団体だけに当てはまるなら、巡は止めずにその団体だけ見送る**（unrecordable に確認日つきで記録し、
  パーサの穴なら `パーサ` ラベルの issue に積む。第2巡の宇都宮・西宮）。巡を止めるのはバッチ全体が進まないとき。
- **出力退化の前兆**（本文と無関係な単語の孤立行）→ 最小限で切り上げ、モデル切替・新セッションを提案する。
- 止まるときは**理由と再開点を handoff §5 に書いてから** `stop`。

## 6. 毎巡読み返す短い一覧

- `main` へ直接 push しない（push ＝ 本番デプロイ）。必ずブランチ → PR → squash。
- 実データのみ。推計・ダミーを置かない。未知の科目を「その他」に寄せない。
- 数字を手書きしない（gen で数える）。
- archive は直列。団体コードは原本。款名は全件目視。
- 年度延伸しない。1巡1PR。積まない。`--delete-branch` を付けない。
- 偵察レポートは docs に残してから終える。
